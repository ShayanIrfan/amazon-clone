# Capture hook scratch state (the logs themselves, .agent-logs/, are committed)
.claude/capture-state/
.claude/settings.local.json

# Recon screenshots show a real Amazon account; kept local only (docs/recon.md is committed)
docs/screenshots/

node_modules/
.env
.env.*
!.env.example
.vercel/
.next/
dist/

# Local agent skill copies from the `npx skills` tool — not app code, kept out of the repo
.agents/
skills-lock.json
