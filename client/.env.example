# Vite app (client/). Only VITE_* vars reach the browser; never put secrets here.
# Stripe test publishable key. Placeholder value = built-in mock payments.
VITE_STRIPE_PUBLISHABLE_KEY=pk_test_REPLACE_ME
